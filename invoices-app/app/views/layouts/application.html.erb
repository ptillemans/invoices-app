<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Invoice Approval Uploader (v#{Rails.application.config.version})" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <div class="grid-container">
      <div class="grid-x grid-margin-x">
        <div class="small-4 columns">
          <p class="version">
            <%= "(v#{Rails.application.config.version})" %>
          </p>
        </div>

        <div class="menu align-right">
          <ul class="button-group">
            <li><%= link_to "Home", root_path, class: 'button' %></li>
            <li><%= link_to "Organizations", organizations_path, class: 'button' %></li>
            <li><%= link_to "Invoices", invoices_path, class: 'button' %></li>
            <li><%= link_to "Users", users_path, class: 'button' %></li>
            <li><%= link_to "Jobs", crono_web_path, class: 'button' %></li>
            <li>
                <% if current_user %>
                    <%= link_to "Log out", log_out_path, class: 'button' %>
                <% else %>
                    <%= link_to "Log in", log_in_path, class: 'button' %>
                <% end %>
            </li>
          </ul>
        </div>
      </div>
    </div>

    <div class="grid-container">
      <div class="grid-x grid-margin-x">
          <% flash.each do |name, msg| -%>
            <div class="callout small-8" data-closable>
                <button class="close-button" aria-label="Close alert" type="button" data-close>
                    <span aria-hidden="true">&times;</span>
                </button>
                <p><%= msg %></p>
            </div>
        <% end -%>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
